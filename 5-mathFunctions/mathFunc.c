#include <stdio.h>
#include <math.h>

int main () {

    // math functions

    double A = sqrt(25);
    double B = pow (2,4);
    int C = round(3.14);
    int D = ceil(3.14);
    int E = floor(3.14);
    //double F = fabs(-100);
    double G = log(3);
    double H = sin(45);
    double I = cos(45);
    double J = tan(45);

    printf("\n%lf", J);

    return 0;

}